version: '3.7'


services:
  db:
    image: postgres
    env_file:
      - .env
    ports:
      - 5454:5432
  web:
    build: .
    command: /bin/bash -c "sleep 10; python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db